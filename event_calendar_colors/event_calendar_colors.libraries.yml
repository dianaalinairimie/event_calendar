# A default library for status color, this will not be used directly, it will be
# used to generate the colors for each status.
event_color_default:
  version: 1.x
  css:
    layout:
      css/event-status-color.css: {}
